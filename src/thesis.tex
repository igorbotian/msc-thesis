% === DOCUMENT PROPERTIES ===

\documentclass[12pt,a4paper,oneside]{article}

\title{Software Security Threats in Mobile Peer-to-peer Environment} % the title of the thesis
\author{Igor Botyan} % the author of the thesis
\date{\today} % the date of the thesis submission

\input{localization}
\input{nomenclature}
\ProduceRussianVersion
%\ProduceEnglishVersion

% === LIST OF USED PACKAGES ===

% to make the thesis pfd TeX package should be installed in the system
% sudo apt-get install texlive-base

% these packages should be placed in the beginning of the packages list 
%\usepackage{ucs} % to enable Unicode support
\usepackage[utf8]{inputenc} % to allow UTF8-characters in the source files
% sudo apt-get install texlive-latex-extra

\usepackage[printonlyused]{acronym} % to make list of abbreviations
\usepackage{bold-extra} % to highlight keywords in source code listings

\usepackage[
	font=small,
	format=plain,
	labelfont=bf,%up,
	textfont=normalfont,%up,
	labelsep=period
]{caption} % to format picture captions

\usepackage{color} % to use various font colors
\usepackage{combelow} % to use 'comma below a letter' special characters
\usepackage{datetime} % to get the current year and to format dates
\usepackage{environ} % to define environments in command style
\usepackage{etoolbox} % to eliminate \hbox badnesses in bibliography area
\usepackage{fancyhdr} % to format page headers
\usepackage{geometry} % to define custom margins of each page
\usepackage{graphicx} % to use png graphic format for pictures
\usepackage{hyperref} % to make references as hyperlinks
\usepackage{ifthen} % provides \ifthenelse test
%\usepackage{lastpage} % to determine a total number of pages of the thesis
\usepackage{lipsum} % to fill the contents of a chapter
\usepackage{listings} % to format listings of source code
\usepackage{multicol} % to use multiple columns
\usepackage{setspace} % to set line spacing
\usepackage{verbatim} % to hide 'comment' environments in output file
\usepackage{xifthen} % provides \isempty test
\usepackage{xstring} % to performs basic string operations

\LoadLanguageSpecificPackages{}

% === TEX DEFINITIONS ===

\include{tex_definitions}
\include{styles}

% === PREFERENCES ===

% to define margins of each page
\geometry{left=4cm} % 3-5 cm
\geometry{right=2cm} % 2 cm
\geometry{top=2.5cm} % 3.5 cm
\geometry{bottom=3.5cm} % 2 cm

\DeclareGraphicsExtensions{.png}

\hypersetup{
	bookmarkstype=none,
	plainpages=false,
	pdftitle={Software Security Threats in Mobile Peer-to-peer Environment},
	pdfauthor={Igor Botyan},
	pdfsubject={Master's Thesis},
	pdfnewwindow=false,
	pdfkeywords={mobile environment, peer-to-peer, software, security threats, PeerHood},
	colorlinks=true,
	linkcolor=black,
	citecolor=black,
	filecolor=black,
	urlcolor=black,
	pageanchor=false} % if 'lastpage' is enabled remove this option 

\onehalfspacing % to set one and half line spacing
\setlength{\parindent}{0pt} % to disable indentation of a new paragraph
%\setlength{\parskip}{\baselineskip} % to set the space between paragraphs

\setlength{\headheight}{28pt}
\ApplyGeneralPageStyle{}

\apptocmd{\thebibliography}{\raggedright}{}{} % to eliminate \hbox badnesses in bibliography area

% === DOCUMENT BODY ===

\begin{document}

\graphicspath{{./images/}} % to set the path to image files

% Preamble

\IncludeChapter{preamble}

% Chapters

\newpage
\IncludeChapter{introduction}

\newpage
\IncludeChapter{software_security}

\newpage
\IncludeChapter{peerhood}

%\newpage
%\IncludeChapter{peerhood_analysis}

%\newpage
%\IncludeChapter{conclusions}

%\newpage
%\include{misc/temporary_chapter}

% References

\newpage
\IncludeChapter{references}

% Appendices

\newpage
\IncludeChapter{appendices}

\end{document}
